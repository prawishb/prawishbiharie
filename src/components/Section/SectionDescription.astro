---
import type { HTMLAttributes } from "astro/types";
import { cn } from "../../lib/utils";

type Props = HTMLAttributes<"p"> & {};

const { class: className } = Astro.props as Props;
---

<p class={cn("text-center text-sm mb-8", className)}><slot /></p>
